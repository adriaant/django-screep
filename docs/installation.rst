============
Installation
============

At the command line::

    $ easy_install django-screep

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-screep
    $ pip install django-screep